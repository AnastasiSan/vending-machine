1.Event Loop (цикл событий)– это механизм, который помогает JS не ждать выполнения задачи, которая требует время, когда есть другие задачи, которые должны выполниться в текущий момент, а выполняет их параллельно.
  Event loop работает следующим образом: при получении задачи он перемещает ее в стек вызовов (call stack). Если функция синхронная, то она сразу же выполняется и удаляется из стека.
  Если функция асинхронная, то управление переходит к Web API. Колбэк асинхронной функции перемещается в Web API, при этом сама функция удаляется из стека вызовов. Web API ожидает триггера наступления определенного события, например истечение времени setTimeout, затем перемещает колбэк в очередь колбэков.
  Когда стек вызовов освобождается, Event Loop перемещает колбэк из очереди колбэков в стек вызовов, после чего событие исполняется и покидает стек. Процесс выполняется циклично, пока не выполнится весь javascript код.

2.Closure(замыкание) – функция внутри другой функции. Функция, в которой лежит другая функция, как бы замыкает в себе значение, которое нужно передать во внутреннюю функцию.

3.Протокол http.

4.Rest – это архитектурный стиль API для сложных интернет сервисов. Его принцип заключается в введении ограничений.
  Каждый ресурс должен иметь один URI, за манипуляцию которого будут отвечать методы запросов GET, POST, PUT, DELETE.
  Сервер должен подсказывать клиенту какие следующие шаги он должен предпринять дальше

5.TypeScript это язык программирования расширяющий возможности JS с добавлением типов(Н-р: string, number и т.д.). Типизация нужна для того, чтобы избегать ошибки на этапе компиляции. То есть, если в какую-нибудь переменную a нам нужно передать только числовые значения, мы можем затипизировать это в interface и если туда передастся строка, то TypeScript укажет нам на эту ошибку. Именно типизация является отличием от JS.


6.DOM (Document Object Model) - это представление документа HTML или XML в виде древовидной структуры, позволяющая скриптам манипулировать содержимым, стилями и структурой документа.
  CSSOM (CSS Object Model) - это модель объектов для работы с CSS на стороне клиента, позволяющая скриптам манипулировать стилями и отображением страницы.

7.В body есть div, в котором лежит кнопка, на все эти три элемента повешено событие клика, при нажатии на кнопку сначала произойдет событие кнопки, потом div’a, а уже потом body – это и называется events bubbling – всплытие.

8.Меня привлекает написание кода, разработка вроде бы лёгкого, но в то же время сложного функционала. Когда долго работаю над задачей, получаю удовольствие от того, что получилось сделать что-то, что я раньше не могла.
  Вообще в программирование я пришла 2 года назад, попав на стажировку в своём городе, тогда я была в этом полный ноль, после этой стажировки(не получилось продолжить работу, так как стажировка была не оплачиваемая, да и знаний было мало) я поняла, что нужно найти курсы и попала в Skillbox
  Я бы хотела глубже изучить React на практике, хочу попробовать работать в команде, понять каково это работать в данной сфере

